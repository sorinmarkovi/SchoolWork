/* 1. Gather requirements
 * 2. Describe the application
 * 3. Identify the main objects
 * 4. Describe interactions
 * 5. Create a class diagram
 */
public class MillionCoins 
{
	public static void main(String[] args) 
	{
		// TODO (Simulation: heads or tails) Write a program that simulates flipping a coin one	million times and displays the number of heads and tails.
		int counter = 0;
		int heads = 0;
		int tails = 0;
		int flipResult;
		 //1000000
		while (counter <100)
		{
		flipResult = coinFlip();
		System.out.println(flipResult);
		counter++;
		}
	}
	public static int coinFlip()
	{
		int counter = 0;
		while (counter < 100)
		{
			int flip = (int) Math.floor(Math.random());
			counter++;
		}
		
		return coinFlip();
	}
}